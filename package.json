{
  "name": "ai-api-toolsite",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "test": "turbo test",
    "db:migrate": "pnpm --filter @ai-toolsite/db prisma migrate deploy",
    "db:seed": "pnpm --filter @ai-toolsite/db prisma db seed"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "pino-pretty": "^13.1.3",
    "turbo": "^2.0.0",
    "typescript": "^5.3.3"
  },
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=20.0.0"
  },
  "dependencies": {
    "volcengine": "^0.0.1",
    "volcengine-node-sdk": "^0.0.1"
  }
}
